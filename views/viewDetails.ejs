<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', sans-serif;
            background: #f8f9fa;
            min-height: 100vh;
            padding: 48px 24px;
            color: #1a1a1a;
            max-width: 1000px;
            margin: 0 auto;
        }

        h1 {
            font-size: 32px;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 24px;
            letter-spacing: -0.5px;
        }

        h3 {
            font-size: 15px;
            font-weight: 400;
            color: #1a1a1a;
            margin-bottom: 24px;
            line-height: 1.6;
        }

        p {
            font-size: 14px;
            color: #1a1a1a;
            margin-bottom: 24px;
        }

        p a {
            color: #1a1a1a;
            text-decoration: underline;
            font-weight: 500;
            transition: all 0.2s ease;
        }

        p a:hover {
            color: #374151;
        }

        h5 {
            font-size: 14px;
            font-weight: 400;
            color: #1a1a1a;
            margin-bottom: 32px;
        }

        iframe {
            width: 100%;
            height: 600px;
            border: 1px solid #e5e7eb;
            border-radius: 12px;
            background: white;
            display: block;
            margin-top: 32px;
        }

        @media (max-width: 768px) {
            body {
                padding: 32px 16px;
            }

            h1 {
                font-size: 28px;
            }

            h3 {
                font-size: 15px;
            }

            iframe {
                height: 500px;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 24px 12px;
            }

            h1 {
                font-size: 24px;
            }

            iframe {
                height: 400px;
            }
        }
    </style>
</head>
<body>
    <h1><%= data.title %></h1>
    <h3>Description:<%= data.description %></h3>
    <p>file: <a href="<%= data.fileName[0] %>">File</a></p>
    <h5>Current Status:<%= data.status %></h5>
    <iframe src="<%= data.fileName[0] %>" frameborder="10"></iframe>
</body>
</html>